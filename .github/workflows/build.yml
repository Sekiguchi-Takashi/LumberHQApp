name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Gradle 8.9
        run: |
          curl -sLO https://services.gradle.org/distributions/gradle-8.9-bin.zip
          unzip -q gradle-8.9-bin.zip -d "$HOME"

      - name: Build debug APK
        run: $HOME/gradle-8.9/bin/gradle assembleDebug --no-daemon

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk LumberHQApp-v1.3-debug.apk

      - uses: actions/upload-artifact@v4
        with:
          name: LumberHQApp-v1.3-debug
          path: LumberHQApp-v1.3-debug.apk
